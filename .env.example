# Copy to .env and set values. Required for server and DB scripts.
# For local dev with Docker Postgres: docker compose up postgres -d
DATABASE_URL=postgres://viziers:viziers@localhost:5432/viziers_vault
