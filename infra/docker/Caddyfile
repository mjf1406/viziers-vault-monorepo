# Reverse proxy placeholder for self-host.
# Uncomment and adjust when running Caddy in Docker Compose.
# Example: proxy / to web:3000, /api to server:3001

# :80 {
#   handle /api/* {
#     reverse_proxy server:3001
#   }
#   handle {
#     reverse_proxy web:3000
#   }
# }
