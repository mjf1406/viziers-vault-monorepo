# desktop

Placeholder for the future **Electron** app shell.

This app will host the same web frontend (or a shared UI) inside an Electron window for desktop distribution. No Electron implementation yet—add when ready.
