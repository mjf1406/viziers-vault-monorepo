# Optional: override API base URL (e.g. for non-standard deployments).
# Leave unset for normal usage: production/self-host use relative /api, local dev uses Vite proxy.
# VITE_API_BASE_URL=http://localhost:3000
