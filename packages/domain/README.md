# @workspace/domain

Shared schemas, permission primitives, and domain types used by both `web` and `server`. No UI or I/O—pure types and validation.

- **schemas/** — Zod (or similar) schemas for API and domain models.
- **permissions/** — Permission checks and role/ability types (app-owned, not auth-provider).
- **events/** — Domain event shapes for future use.
