# @workspace/auth

Thin boundary package around **Better Auth**. Exposes server and client auth setup and shared types.

- **Permissions** live in app/domain logic, not in this package.
- TODO: Wire Better Auth providers and database adapter when implementing auth flows.
